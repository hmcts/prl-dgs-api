spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/div
      aliases:
        prl.microservicekey-prl-dgs-api: AUTH_PROVIDER_SERVICE_CLIENT_KEY
        prl.docmosis-api-key: DOCMOSIS_SERVICE_ACCESS_KEY
        prl.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
